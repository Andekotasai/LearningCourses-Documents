import { Routes } from '@angular/router';
import { TwoWayBindingSignal } from './two-way-binding-signal/two-way-binding-signal';

export const routes: Routes = [
    {
        path:'Binding',
        component:TwoWayBindingSignal
    }
];
